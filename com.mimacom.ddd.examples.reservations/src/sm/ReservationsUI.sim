system sm.res


import sm.ReservationsBase

interface information model ReservationsUI {

	//grab aggregate dm.Reservations.Tables { } 
	
	grab root entity dm.Reservations.Party as SParty 
	add { 
		id : ID
	} 
	 
	morph root entity dm.Reservations.Table to detail STable 
	add {
		id : ID
	} 
}

